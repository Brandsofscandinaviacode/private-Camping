# Database (SQLite via libsql)
DATABASE_URL="file:prisma/dev.db"

# Session encryption key (mindst 32 tegn - generer med: openssl rand -hex 32)
SESSION_SECRET="change-this-to-a-random-32-character-string!!"
